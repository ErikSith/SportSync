import { BrandAppBar } from '@/components/shared/BrandAppBar';

/** @deprecated Prefer BrandAppBar — kept for existing lobby imports. */
export function LobbyTopBar() {
  return <BrandAppBar accent="primary" />;
}
