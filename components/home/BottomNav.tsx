/**
 * @deprecated Replaced by ThumbButton floating nav.
 * Kept as a no-op so any leftover imports do not render the old bottom bar.
 */
export function BottomNav(_props?: { active?: string }) {
  return null;
}
